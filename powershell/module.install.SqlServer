Install "SqlServer" module
Notes:
  -Guidance @ https://docs.microsoft.com/en-us/sql/powershell/download-sql-server-ps-module?view=sql-server-2017
  -"SQLPS" is an older SQL server module that is installed with SQL Server
  -"CurrentUser" is a powershell keyword that gets the current user
Main Success Scenario:
  1. Install-Module -Name SqlServer
Alternatives:
  1a. Current User is not an admin on the machine
    1a1. Install-Module -Name SqlServer -Scope CurrentUser
